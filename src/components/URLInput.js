import React from 'react';

const URLInput = () => {
    return (
        <form>
            <input type="text"></input>
            <button>Submit</button>
        </form>
    )
}

export default URLInput;